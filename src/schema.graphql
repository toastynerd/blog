type Query {
  latest: [Post]!
}

type Post {
  id: ID!
  title: String!
  content: String!
  createdAt: String!
  publishedAt: String
  author: User!
}

type User {
  email: String!
  password: String!
  name: String!
  posts: [Post]!
}
